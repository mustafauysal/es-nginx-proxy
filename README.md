# NGINX HTTP auth for Elasticsearch

run `docker-compose up -d` for the setting up. 
Default user and password `admin:admin`

You can check url : `http://admin:admin@server-ip-address`

That's all!

`¯\_(ツ)_/¯`